
<%- include('./includ/layout-start')%>

<main>
    <p class="h1" style="padding: 5px">Library</p>
    <div class="container text-right" >
        <a href="index/create"><button type="button" class="btn btn-primary">Create book</button></a>
    </div>
    <div class="container text-center" >
        <ul class="list-group list-group-flush">
            <% data.forEach(item => { %>
                <div class="container text-center">
                    <div class="row">
                      <div class="col-sm-2">
                        <li class="list-group-item"><%= item.title %></li>
                      </div>
                      <div class="col-sm-9">
                        <div class="row">
                          <div class="col-8 col-xs-6">
                            <form action="index/delete/<%= item.id %>" method="POST" class="d-inline">
                              <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                            <a href="index/edit/<%= item.id %>"><button type="button" class="btn btn-info">Edit</button></a>
                            <a href="index/<%= item.id %>"><button type="button" class="btn btn-success">Description</button></a>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
            <% }) %>
          </ul>
    </div>
</main>

<%- include('./includ/layout-end')%>